export interface ServeStaticFileOptions {
  // Specifies the root directory from which to serve static files
  root: string;
  // Specifies the base path to be used when serving static files
  basePath?: string;
}
